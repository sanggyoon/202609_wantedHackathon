services:
  frontend:
    build: ./frontend
    environment:
      - VIRTUAL_HOST=${VIRTUAL_HOST}
      - LETSENCRYPT_HOST=${LETSENCRYPT_HOST}
    restart: always
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: proxy
