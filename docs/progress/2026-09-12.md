# 2026-09-12 진행상황

`docs/progress/2026-09-08.md`의 "다음에 할 일" 중 DB 관련 항목을 진행했다.
**DB 스키마를 설계·구현·검증하고 Supabase Cloud에 적용**했다. 백엔드 애플리케이션 코드는
아직 없다.

작업 브랜치: `feat/supabase-schema` · PR [#4](https://github.com/sanggyoon/202609_wantedHackathon/pull/4)

---

## 1. Supabase GitHub 연동으로 생긴 `supabase/migrations`의 정체 파악

Supabase ↔ GitHub 연동을 켜면서 `supabase/migrations/`가 생겼는데, 이게 무엇인지부터
정리했다. 흔히 아는 "데이터를 다른 플랫폼으로 옮기는" 마이그레이션이 아니라
**DB 스키마 변경 이력을 SQL 파일로 누적 관리하는 것**이다.

- 파일명 앞의 타임스탬프(`20260910124408_init.sql`)가 실행 순서다.
- Supabase는 원격 DB의 `supabase_migrations.schema_migrations` 테이블에 "어디까지
  적용했는지"를 기록해두고, **아직 적용 안 된 파일만** 순서대로 실행한다.
- 따라서 **한번 푸시한 마이그레이션 파일은 수정하면 안 된다.** 스키마를 바꾸려면
  새 파일을 추가한다.
- 원격은 앞으로만 쌓이고(데이터 보존), 로컬은 `supabase db reset`으로 0부터 재구축
  (데이터 소멸)한다. `--linked` 플래그를 붙이면 **원격을 날리므로** 습관적으로 쓰면 안 된다.

개념상으로는 IaC(Infrastructure as Code)의 하위 개념인 *database schema as code*에
해당한다. 단 Terraform 같은 선언형이 아니라 **명령형·누적형**이라, 파일 하나만 봐서는
현재 스키마를 알 수 없고 전체를 순서대로 재생해야 한다.

GitHub Actions와는 역할 층위가 다르다. Actions는 "언제 무엇을 실행할지"를 정하는
엔진이고(`ci.yml`은 lint/build만 담당, DB 얘기는 한 줄도 없음), 마이그레이션은
Supabase 서버가 읽어 실행하는 내용물이다. 둘은 서로를 모르는 별개 파이프라인이다.

## 2. 데이터 플로우 명세를 레포에 포함 — `docs/Data_Flow.md` (v0.2)

채팅으로만 공유되던 데이터 플로우 명세(v0.1)를 레포에 넣고, 아래 §3에서 확정한 내용을
반영해 v0.2로 갱신했다.

**v0.1의 내부 모순을 하나 발견해 해소했다.** §5 Level 1 DFD는 `P1. 사건/링크 발급`이
`status=DRAFT`로 D1을 생성한다고 표시하는데, §6 데이터 스토어 표는 D1 생성 시점을
"A가 고소장 확정 시"로 적어 서로 어긋났다. **§5를 채택** — 링크를 발급하려면 토큰
해시로 조회할 행이 이미 존재해야 한다. §6 표를 고치고 인용 블록으로 근거를 남겼다.

그 외 v0.2 변경:

- §8.3 **미답변 만료 경로** 추가 (B가 영영 답하지 않은 사건, A가 `DRAFT`로 이탈한 사건)
- §9 검수 매핑에 **"보장 주체"** 열 추가 — 스키마가 구조적으로 막는 항목과 구현에서
  확인해야 하는 항목을 구분. 예: "원문이 DB에 저장되지 않는다"는 스키마가 보장하지만
  "원문이 로그에 저장되지 않는다"는 코드 리뷰 몫이다. v0.1은 이 둘이 한 줄에 뭉쳐 있어
  후자를 놓치기 쉬웠다.
- §11 **구현 매핑** 추가 (각 요소가 `supabase/migrations/` / `backend/app/` /
  `frontend/src/` 중 어디로 가는지)
- 미결 사항 **8건 → 4건**

## 3. 스키마 설계 확정 — `docs/Supabase_Schema_Design.md`

데이터 플로우 명세 v0.1이 "착수 전 확정 필요"로 남긴 8건 중, 스키마 모양을 바꾸는
항목을 결정했다.

| # | 결정 | 비고 |
| --- | --- | --- |
| D-1 | `expires_at`은 **최종 답변 시각 + 7일** | 생성 시 `created_at + 7일`로 초기화 후 답변 시 갱신 |
| D-2 | 만료 삭제는 **pg_cron**이 실행 | 백엔드 스케줄러 대신. ADR §10 방향과 일치 |
| D-3 | B 답변은 **1회만, 제출 즉시 읽기 전용** | DB 제약으로 강제 |
| D-4 | 링크 폐기 기능 **MVP 제외** | PRD §15 범위 밖 |
| D-5 | 원문 임시 저장 테이블 **없음** (서버 무저장) | |
| D-6 | 토큰은 **SHA-256 hex**, 만료 후에도 `cases` 행 유지 | 토큰 재사용 방지 |
| D-7 | DB 접근자는 **FastAPI 단독** | 브라우저는 Supabase에 직접 붙지 않음 |

**D-1에 함정이 있었다.** "최종 답변 기준"만 쓰면 B가 영영 답하지 않은 사건은 만료
시각이 존재하지 않아 영구히 남는다. 그래서 생성 시 `created_at + 7일`로 깔아두고
답변 시 갱신하는 2단계 구조가 됐다. 사건 총 수명 상한은 (B 대기 기간 + 7일)이고,
리포트가 존재하는 기간은 항상 7일이다.

### 테이블 구조

```
cases (D1)
 ├─ statement_cards   (D2)  UNIQUE(case_id, side)  — A/B 각 1장
 ├─ apologies         (D3)  PK = case_id           — 사건당 1장
 └─ mediation_reports (D4)  PK = case_id           — 사건당 1장
```

`apologies`와 `mediation_reports`는 **`case_id` 자체를 기본키로** 뒀다. 별도 제약 없이
"사건당 1건"이 DB 차원에서 강제되므로 D-3(1회 제출)이 앱 코드와 무관하게 보장된다.
`statement_cards`는 A·B 두 장이라 `UNIQUE(case_id, side)`로 같은 효과를 낸다.

PRD §11 초안에 없던 컬럼 둘을 추가했다 — `cases.answered_at`(D-1의 기준 시각),
`cases.purged_at`(삭제 완료를 검수로 증명하기 위한 증거).

### RLS

4개 테이블 모두 RLS를 켜되 **정책은 하나도 만들지 않는다.** RLS가 켜졌는데 정책이
없으면 모든 행이 거부되므로 `anon`·`authenticated` 키로는 아무것도 못 읽고,
`service_role`만 우회해서 통과한다. 프론트에 anon 키가 실수로 노출돼도 데이터가 새지
않는다.

"링크가 곧 권한"은 DB가 아니라 **FastAPI가 토큰 해시로 조회하는 계층**에서 구현한다.
RLS로 토큰을 검증하려면 요청마다 세션 변수를 세팅해야 해서 단일 접근자 구조에서는
복잡도만 늘어난다.

### 상태 전이 제약에 대한 방침

PK·UNIQUE 외에 상태 전이를 강제하는 CHECK 제약은 **일부러 두지 않았다.** 3주 일정에서
상태 머신이 아직 흔들릴 수 있고, 과도한 제약은 정당한 전이까지 막아 디버깅 비용을
키운다. 대신 조건부 UPDATE로 처리한다.

```sql
update cases
   set status = 'COUNTER_COMPLETED', answered_at = now(),
       expires_at = now() + interval '7 days'
 where id = $1 and status = 'COUNTER_DRAFT';
```

영향 행이 0이면 이미 전이된 것이므로 중복 제출로 간주한다. 결과물 테이블의 PK·UNIQUE가
최후의 방어선이다.

## 4. 문서 배치 정리

설계 스펙을 `docs/superpowers/specs/` 아래에 만들었다가 `docs/` 바로 아래로 옮기고
`superpowers/` 디렉터리를 삭제했다. 파일명도 기존 `docs/` 규칙(`Tech_ADR.md`,
`Design_System.md` 등 Title_Case)에 맞춰 `Supabase_Schema_Design.md`로 바꿨다.
`Data_Flow.md`의 참조 경로 2곳도 함께 수정했다.

문서 간 인용 규칙도 정했다. `Data_Flow.md`와 `Tech_ADR.md`에 **둘 다 §10이 있어**
(전자는 "미결 사항", 후자는 "DB 마이그레이션 도구") 혼동이 있었다. 앞으로는
`[DFD §10]`, `[ADR §10]`처럼 출처를 붙인다.

## 5. 마이그레이션 SQL 작성 — `supabase/migrations/` 2개

§3의 설계를 SQL로 옮겼다.

| 파일 | 내용 |
| --- | --- |
| `20260912113000_case_schema.sql` | 열거형 3개, 테이블 4개, 부분 인덱스, RLS |
| `20260912113100_expiry_purge.sql` | `purge_expired_cases()`, pg_cron 매시 스케줄 |

**파일을 2개로 나눈 이유**는 `pg_cron` 활성화가 Supabase에서 설치 스키마 제약으로
실패할 여지가 있어서다. 분리해두면 두 번째가 깨져도 핵심 스키마는 적용된 채로 남고,
대시보드 Database > Extensions에서 수동으로 켠 뒤 두 번째만 재시도하면 된다.

### 설계 스펙에서 벗어난 점

스펙 §4.3에 있던 `statement_cards(case_id)` 단독 인덱스를 **제거했다.**
`UNIQUE(case_id, side)`가 만드는 btree 인덱스의 선두 컬럼이 이미 `case_id`라
`where case_id = ?` 조회와 FK cascade 삭제를 모두 커버한다. 별도 인덱스는 쓰기 비용만
늘리는 중복이었다. 스펙에도 근거를 적어 반영했다.

### 구현 시 붙인 안전장치

- `purge_expired_cases()`를 `security definer` + `set search_path = public, pg_temp`로
  고정 — cron 잡 실행 주체와 무관하게 동일 권한으로 동작시키고 탐색 경로 하이재킹을 막는다.
  함수의 기본 `PUBLIC` 실행 권한은 회수했다.
- 스케줄 등록 앞에 `cron.unschedule` 가드 — Preview Branch 재생성 등으로 재실행돼도
  잡이 중복 등록되지 않는다.
- `gen_random_uuid()`는 PG13+ 코어(`pg_catalog`) 내장 함수다. `init.sql`이 `pgcrypto`를
  `extensions` 스키마에 설치했지만 search_path와 무관하게 동작한다 (`config.toml`상 PG17).

### 작성 시점의 검증 상태 — 미검증

작성 직후에는 이 SQL이 한 번도 실행된 적이 없었다. 로컬에 Postgres도 `psql`도 없고
Docker 데몬도 꺼져 있어 문법 검사를 못 했다.

순정 `postgres:17` 컨테이너로 미리 돌려보는 안을 검토했으나, 정작 가장 불확실한
`pg_cron`이 순정 이미지에 없어 검증 범위 밖이고 **PR의 Supabase Preview Branch가
실제 환경에서 전부 검증해줄 것**이라 판단해 PR로 직행했다.

이 판단은 틀렸다. §6 참고.

## 6. PR 생성과 로컬 검증 — PR [#4](https://github.com/sanggyoon/202609_wantedHackathon/pull/4)

### 6.1 `gh` CLI 도입

PR을 만들려는데 `gh`가 없어 `brew install gh`로 설치하고 인증했다.

인증 직후 확인했을 때 `gh auth status`가 "not logged into any GitHub hosts"로 나와
잠시 혼선이 있었다. 원인은 **토큰이 macOS 키체인(keyring)에 저장되어
`~/.config/gh/hosts.yml`에 평문으로 남지 않기 때문**이었다. 설정 파일 존재 여부로
인증 상태를 판단한 것이 잘못이었다.

인증 확인은 파일이 아니라 다음 두 명령으로 한다. 두 번째가 실제 검증이다 — 저장된
자격증명을 읽는 데 그치지 않고 그 토큰으로 실제 요청을 보내 만료·폐기까지 걸러낸다.

```bash
gh auth status           # 로그인 상태·계정·스코프
gh api user --jq .login  # 실제 API 호출까지 되는지
```

앞으로 PR·이슈·Actions 로그 확인을 CLI로 할 수 있다. `2026-09-08` 때 CD 실패 로그를
API로 못 읽어 웹 UI에서 직접 확인했던 문제도 이걸로 해소된다.

### 6.2 Preview Branch가 돌지 않았다

PR 생성 후 체크 결과:

```
backend   pass  11s
frontend  pass  26s
```

`ci.yml`의 lint/build 두 개가 전부였다. **Supabase 관련 체크·commit status·PR 코멘트가
하나도 없었다**(commit status 0개, 코멘트 0개). 즉 Preview Branch가 생성되지 않았다.

Supabase **Branching이 Pro 플랜 이상 기능**이라 무료 플랜에서는 연동을 켜도 `main` push
시 자동 적용만 되고 PR별 임시 DB는 만들어지지 않는 것으로 보인다. 대시보드 Branches에서
확인이 필요하다.

> **이 추정은 틀렸다.** 자동 적용도 함께 동작하지 않았다. §7.2 참고.

> **`docs/Tech_ADR.md` §10 보정 필요.** ADR §10은 Supabase GitHub 연동의 채택 근거 중
> 하나로 "PR마다 격리된 Preview Branch(임시 DB)를 자동 생성한다"를 들었는데, 현재 플랜에서는
> 이 이점이 실제로 성립하지 않는다. 나머지 근거(백엔드 의존성 0, SQL 직접 작성, 기존
> 파이프라인 무수정, ADR §6과의 방향 일치)는 그대로 유효하므로 **결정 자체는 뒤집을 필요가
> 없으나, 근거 한 줄은 사실과 맞지 않는다.** ADR 수정은 팀 합의 후 별도로 한다.

이로써 **머지 전 검증 수단이 사라졌다.** 마이그레이션이 검증 없이 프로덕션에 바로 적용되고,
`case_schema.sql` 중간에서 실패하면 테이블 일부만 생긴 상태로 멈출 수 있다. 그래서 §5에서
"실익이 적다"며 건너뛴 로컬 Docker 검증을 다시 꺼내 실행했다.

### 6.3 로컬 검증 방법

순정 `postgres:17` 컨테이너(PostgreSQL 17.11)를 띄우고 마이그레이션을 순서대로 적용했다.
Supabase 환경과의 차이는 두 가지를 채워서 메웠다.

| 차이 | 대응 |
| --- | --- |
| `extensions` 스키마 부재 (`init.sql`이 요구) | `create schema extensions` 선행 |
| `anon`·`authenticated`·`service_role` 롤 부재 | 테스트용으로 생성 |
| `pg_cron` 확장 부재 | 해당 3줄을 제거한 사본으로 적용 |

적용 순서 — stub → `20260910124408_init.sql` → `20260912113000_case_schema.sql` →
`20260912113100_expiry_purge.sql`(pg_cron 제거판). **네 개 모두 에러 없이 적용됐다.**

### 6.4 검증 결과 — 전부 통과

| # | 검증 항목 | 기대 | 결과 |
| --- | --- | --- | --- |
| 1 | 4개 테이블 RLS 활성화, 정책 0개 | `relrowsecurity=t`, 정책 0 | ✅ |
| 3 | 같은 사건에 A 카드 중복 삽입 | `unique_violation` | ✅ 거부 |
| 4 | 사과문 중복 제출 | `unique_violation` | ✅ 거부 |
| 6 | `purge_expired_cases()` 반환값 | `1` (만료 사건 1건) | ✅ |
| 7 | 만료 사건만 결과물 삭제 | 아래 참조 | ✅ |
| 8 | 재실행 멱등성 | `0` | ✅ |
| 9 | `cases` 삭제 시 cascade | 자식 0건 | ✅ |
| 10 | 부분 인덱스 사용 | Index Scan | ✅ (6.5 참조) |

**7번이 핵심 검증이다.** 만료된 사건과 미만료 사건을 하나씩 만들고 purge를 돌렸다.

```
               case_id                |      status       | purged | cards | apologies | reports
--------------------------------------+-------------------+--------+-------+-----------+---------
 11111111-1111-1111-1111-111111111111 | EXPIRED           | t      |     0 |         0 |       0
 22222222-2222-2222-2222-222222222222 | APOLOGY_COMPLETED | f      |     1 |         1 |       0
```

만료 사건은 카드 2장·리포트 1개가 모두 삭제되고 `EXPIRED` 전이와 `purged_at` 기록이
일어났다. 미만료 사건은 카드 1장·사과문 1개가 그대로 남았다. "만료된 것만 지운다"가
실제로 동작한다.

3·4번은 D-3(1회 제출 + 즉시 잠금)이 애플리케이션 코드 없이 DB 제약만으로 지켜지는지를
확인한 것이다. 둘 다 `unique_violation`으로 거부됐다.

### 6.5 부분 인덱스 — 처음엔 안 쓰이는 것처럼 보였다

`explain`을 처음 돌렸을 때 `Seq Scan`이 나왔다.

```
Seq Scan on cases
  Filter: ((status <> 'EXPIRED'::case_status) AND (expires_at <= now()))
```

인덱스가 안 먹는 것처럼 보이지만, 당시 테이블에 행이 2개뿐이라 플래너가 합리적으로 고른
결과였다. 두 가지로 교차 확인했다.

- `set enable_seqscan = off` → `Bitmap Index Scan on cases_expires_at_idx` 사용 확인
- 행 5,000개(그중 4,500개 `EXPIRED`) 주입 후 `analyze` → 자연스럽게 인덱스 선택

```
Index Scan using cases_expires_at_idx on cases
  Index Cond: (expires_at <= now())
```

부분 인덱스가 의도대로 동작한다. `EXPIRED` 4,500행은 애초에 인덱스에 포함되지 않으므로
만료 배치가 훑을 일이 없다.

### 6.6 검증하지 못한 것

`pg_cron` 관련 3줄 — `create extension if not exists pg_cron`,
`cron.unschedule` 가드, `cron.schedule` 호출 — 은 순정 이미지에 확장이 없어 제외했다.
**검증된 것은 `purge_expired_cases()` 함수 본체(plpgsql 문법 + 실제 삭제 동작)까지이고,
확장 활성화와 스케줄 등록은 Supabase에서만 확인된다.** 마이그레이션을 2개로 분리해둔
이유가 이것이다.

### 6.7 부수적으로 알게 된 것

`expiry_purge.sql`의 `revoke all on function ... from anon, authenticated`는 **해당 롤이
존재해야 동작한다.** 순정 Postgres에는 없어 테스트용으로 만들어줘야 했다. Supabase에는
기본 제공되므로 문제는 없으나, 이 마이그레이션이 사실상 Supabase 전용이라는 뜻이기도 하다.
다른 Postgres로 옮길 일이 생기면 이 두 줄이 걸린다.

검증에 쓴 컨테이너는 삭제했다. 프로젝트에 남은 것은 없다.

## 7. 머지 후 — 적용이 안 되고 있었음을 발견, CLI로 전환

PR #4를 머지하고(13:25 UTC) 실제 반영을 확인하려 SQL Editor에서 마이그레이션 이력을
조회했는데 테이블 자체가 없었다.

```
ERROR: 42P01: relation "supabase_migrations.schema_migrations" does not exist
```

이 테이블은 마이그레이션이 한 번이라도 적용되면 Supabase가 자동 생성한다. **부재 자체가
"한 번도 적용된 적 없음"의 확정 증거다.** 추가 조회 결과:

| 확인 | 값 | 의미 |
| --- | --- | --- |
| `supabase_migrations` 스키마 | 없음 | 적용 이력 0 |
| `public` 테이블 수 | 0 | 오늘 머지분 미적용 |
| `pgcrypto` | 있음 | **함정** — 아래 참고 |
| `pg_cron` | 없음 | 당연 |

09-10 연동 시점부터 09-12까지 **마이그레이션이 한 번도 적용되지 않았다.** `init.sql`이
`pgcrypto` 확장 한 줄뿐이어서 아무도 눈치채지 못했다.

### 7.1 판단을 흐린 우연

`pgcrypto`가 활성 상태인 것을 보고 "`init.sql`은 적용됐구나"로 읽을 수 있었다. 실제로는
**Supabase가 새 프로젝트에 기본 제공하는 것**이었다. 마이그레이션이 활성화하려던 것과
같은 확장이라 구별이 안 됐다.

`supabase_migrations` 스키마가 없다는 사실이 있어서 역으로 판별할 수 있었다 — 이력이 없으면
`init.sql`은 실행된 적이 없고, 따라서 그 `pgcrypto`는 다른 출처다.

**교훈.** 적용 여부는 결과 객체의 존재로 판단하지 말고 `schema_migrations`로 판단한다.

### 7.2 원인 — Branching이 Pro 플랜 전용

GitHub 연동의 **마이그레이션 자동 적용과 Preview Branch는 둘 다 Branching 기능에 속하고,
Branching은 Pro 플랜 이상**이다. 무료 플랜에서는 연동을 켜도 `supabase/` 디렉터리를
스캐폴딩해주는 것까지가 전부다.

§6.2에서 "Preview Branch만 안 되는 것이고 결정 자체는 뒤집을 필요 없다"고 적었으나
**이는 문제 범위를 과소평가한 것이었다.** 핵심 기능인 자동 적용도 같은 뿌리에서 함께
동작하지 않았다. 별개 문제 두 개가 아니라 하나였다.

### 7.3 Supabase CLI로 적용

`brew install supabase/tap/supabase` (2.117.0) 후 로그인·링크·푸시.

`supabase login`은 **TTY가 필요**해 Claude Code의 `!` 프리픽스로는 실행되지 않는다
(`LegacyLoginMissingTokenError`). 실제 터미널 창에서 처리했다. `--token` 플래그나
`SUPABASE_ACCESS_TOKEN` 환경변수 경로는 토큰이 셸 히스토리·로그에 남아 택하지 않았다.

```bash
supabase login
supabase link --project-ref sudpsoonbmnjamechskc   # wanted-hackathon-mediation, ap-southeast-2
supabase db push
```

`db push` 결과 — 3개 모두 적용:

```
Applying migration 20260910124408_init.sql...
Applying migration 20260912113000_case_schema.sql...
Applying migration 20260912113100_expiry_purge.sql...
Finished supabase db push.
```

### 7.4 원격 적용 검증

| 방법 | 결과 |
| --- | --- |
| `supabase migration list` | ✅ 3개 모두 로컬·원격 버전 일치 |
| `supabase inspect db table-stats` | ✅ 테이블 4개, 인덱스 크기가 예상 개수와 일치 |
| `supabase db diff --linked` | ✅ **`No schema changes found`** — drift 0 |
| `supabase db dump --linked` | ✅ RLS 4개 활성·정책 0개, `pg_cron` 확장, `purge_expired_cases()` 확인 |

`db diff --linked`가 가장 강한 검증이다. CLI가 로컬 shadow DB에 마이그레이션을 새로 적용해
만든 스키마와 실제 원격 스키마를 비교하는데, 차이가 0이라는 것은 **마이그레이션 파일만으로
현재 원격 상태가 정확히 재현된다**는 뜻이다.

덤프에서 확인된 것:

```sql
ALTER TABLE "public"."apologies" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "public"."cases" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "public"."mediation_reports" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "public"."statement_cards" ENABLE ROW LEVEL SECURITY;
-- CREATE POLICY 0개
CREATE EXTENSION IF NOT EXISTS "pg_cron" WITH SCHEMA "pg_catalog";
CREATE OR REPLACE FUNCTION "public"."purge_expired_cases"() RETURNS integer
    LANGUAGE "plpgsql" SECURITY DEFINER
    SET "search_path" TO 'public', 'pg_temp'
```

**`pg_cron`은 무료 플랜에서도 문제없이 켜졌다.** 스키마를 명시하지 않았는데 Supabase가
`pg_catalog`에 배치했다. §5·§6에서 가장 불확실하다고 본 부분이 실제로는 통과했고,
동작하지 않은 것은 Branching이었다.

직접 조회하지 않은 항목이 하나 있다 — `cron.job`에 잡이 `active=t`로 등록됐는지. 다만
마이그레이션은 트랜잭션으로 적용되므로 `cron.schedule` 호출이 실패했다면 `expiry_purge.sql`
전체가 롤백되고 push가 실패했을 것이다. 성공한 사실로 강하게 추론되나, 확인은 남겨둔다.

### 7.5 문서 수정

| 문서 | 수정 |
| --- | --- |
| `docs/Tech_ADR.md` §10 | 제목을 "Supabase CLI"로 변경, 번복 경위·발견 경위 추가, 비교 표 3행 갱신, 무효해진 근거에 취소선 |
| `docs/Tech_ADR.md` 미해결 | "마이그레이션 적용 자동화" 항목 추가 |
| `docs/Supabase_Schema_Design.md` §8 | 적용 절차를 CLI 기준으로 재작성, 적용 이력 표 추가 |
| `docs/Supabase_Schema_Design.md` §6.2·§7 | `pg_cron` 실제 결과 반영 |

ADR §10에서 무효해진 근거는 지우지 않고 취소선으로 남겼다.

> ~~이미 동작 중인 CI/CD 파이프라인에 마이그레이션 실행 스텝을 끼워 넣지 않아도 됨~~
> → **이 근거는 더 이상 성립하지 않는다.**

이 근거는 Alembic을 기각한 이유 중 하나였다. 없어진 지금 남은 우위는 "백엔드 의존성 0"과
"SQL 직접 작성" 둘이고, 3주 일정에서는 그 둘만으로도 SQLAlchemy 신규 편입 비용을 넘지
못한다고 보아 **결정 자체는 유지**했다. 다만 `deploy.yml`에 `db push` 스텝을 넣는 쪽으로
가면 Alembic 기각 근거가 거의 사라지므로, 그 판단은 팀 논의로 남겼다.

---

## 결정하지 않고 남긴 것

| # | 항목 | 정해야 할 시점 |
| --- | --- | --- |
| 1 | 만료 후 캐시·백업 삭제 완료 허용 시간 | 서비스 오픈 전 |
| 2 | 외부 AI 제공자의 요청 보관·학습·모니터링 정책 | 서비스 오픈 전 |
| 3 | 외부 AI 제공자에 무보관(zero-retention) 옵션 필수 적용 여부 | 서비스 오픈 전 |
| 4 | 작성 중 임시 저장을 브라우저 로컬에 구현할지 | B 응답 화면 구현 전 |

넷 다 스키마 작업의 선행 조건은 아니다. 3은 Supabase 백업 보존 주기 확인이 선행돼야 한다.

---

## 다음에 할 일

- **팀에 공유할 것** — 마이그레이션 추가 후 `supabase db push`를 반드시 실행해야 한다.
  머지만으로는 DB가 바뀌지 않는다 (§7.2). 09-10~09-12에 실제로 이 일이 일어났다.
  - 팀원 각자 `supabase login` + `supabase link --project-ref sudpsoonbmnjamechskc` 필요
- `cron.job`에 `purge-expired-cases`가 `active=t`로 등록됐는지 확인 (§7.4)
- **마이그레이션 적용 자동화 논의** — `deploy.yml`에 `supabase db push` 스텝을 넣을지.
  넣으면 CI에 Supabase access token·DB 비밀번호 시크릿이 필요하고, Alembic 기각 근거가
  거의 사라진다 (§7.5)
- PR #4 사후 리뷰 — D-4(링크 폐기 기능 MVP 제외)에 대한 팀 동의, PRD §11 필드 누락 여부
- 백엔드에 DB 접근 계층 추가 (현재 `backend/app/`엔 DB 라이브러리가 전혀 없음)
  - 토큰 해시 생성·검증, **만료 lazy 검사**(배치는 최대 1시간 지연되므로 필수),
    상태 전이 조건부 UPDATE
- ~~백엔드에 Alembic 마이그레이션 셋업~~ → ADR §10에서 Supabase GitHub 연동으로 결정됨
- 인증(로그인) 방식·LLM 제공자 — 여전히 미해결
- git config 사용자 정보 정리 — 사용자 본인 작업으로 남겨둠(계속 보류 중)
