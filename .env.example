# 루트 .env — docker-compose.yml의 ${...} 치환에만 쓰인다.
# 백엔드 컨테이너로 들어가는 값(OPENAI_API_KEY, DATABASE_URL 등)은
# 여기가 아니라 backend/.env에 둔다 (docker-compose.yml의 env_file 참고).

VIRTUAL_HOST=wanted.sanggyoon.com
LETSENCRYPT_HOST=wanted.sanggyoon.com
LETSENCRYPT_EMAIL=your-email@example.com
